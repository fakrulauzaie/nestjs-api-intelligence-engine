{
  "$schema": "../../../schemas/api-intel.config.schema.json",
  "version": 1,
  "rules": {
    "no-repository-access-in-controller": "warn",
    "require-guard-on-write-endpoint": ["warn", { "onUnknown": "warn" }],
    "require-complete-write-trace": ["warn", { "onUnknown": "warn" }]
  }
}
